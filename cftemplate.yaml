# This AWS SAM template has been generated from your function's configuration. If
# your function has one or more triggers, note that the AWS resources associated
# with these triggers aren't fully specified in this template and include
# placeholder values. Open this template in AWS Application Composer or your
# favorite IDE and modify it to specify a serverless application with other AWS
# resources.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model template describing your function.
Resources:
  Gettoken:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: sample_lambda_function-dev
      ImageUri: 590183764012.dkr.ecr.ap-south-1.amazonaws.com/demo_repo:node_todo_app
      Description: ''
      MemorySize: 128
      Timeout: 600
      Architectures:
        - x86_64
      EphemeralStorage:
        Size: 512
      EventInvokeConfig:
        MaximumEventAgeInSeconds: 21600
        MaximumRetryAttempts: 2
      #Layers:
        #- arn:aws:lambda:ap-south-1:339713002425:layer:axios:2
        #- arn:aws:lambda:ap-south-1:339713002425:layer:qs:2
      PackageType: Image
      Role: 'arn:aws:iam::590183764012:role/lambda_basic_execution_role'
